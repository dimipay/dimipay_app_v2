# Source - https://stackoverflow.com/a/63009069
# Posted by DannyB, modified by community. See post 'Timeline' for change history
# Retrieved 2026-03-01, License - CC BY-SA 4.0

name: Show Me the S3cr3tz
on: [push]

jobs:
  debug:
    name: Debug
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v2

      - name: Set up secret file
        env:
          DEBUG_PASSWORD: ${{ secrets.MATCH_PASSWORD }}
        run: |
          echo $DEBUG_PASSWORD >> secrets.txt

      - name: Run tmate
        uses: mxschmitt/action-tmate@v2
